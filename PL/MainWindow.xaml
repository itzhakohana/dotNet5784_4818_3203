<Window x:Class="PL.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="MainWindow" Height="720" Width="1280">
    <Window.Resources>
    </Window.Resources>

    <Grid Background="{StaticResource mainBackround}">
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="150"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <!--Grid for the headline-->
        <Grid Grid.Row="1" Grid.Column="1" VerticalAlignment="Stretch" Margin="50,10" HorizontalAlignment="Stretch">
            <Rectangle Grid.RowSpan = "3" Fill="gray" Opacity="0.15" StrokeThickness="0" RadiusX="10" RadiusY="10"/>
            <Label Content="Main Admin Window" Grid.Row="1" Grid.Column="1" FontSize="55" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"
                   Foreground="White" FontWeight="SemiBold" FontStyle="Italic" FontFamily="Global Sans Serif"/>
        </Grid>
        
        <!--Grid which contains the buttons-->
        <Grid Grid.Row="2" Grid.Column="0" Margin="10,15,10,50">
            <Grid.RowDefinitions>
                <RowDefinition Height="3*"/>
                <RowDefinition Height="3*"/>
                <RowDefinition Height="3*"/>
            </Grid.RowDefinitions>
            <!--Left-side buttons-->
            <Rectangle Grid.RowSpan = "3" Fill="gray" Opacity="0.15" StrokeThickness="0" RadiusX="10" RadiusY="10"/>
            <Button Click="showEngineerList_btnClick" Content="Handle Engineers" Style="{DynamicResource mainButtons}" Grid.Row="0" Margin="5,12"/>
            <Button Content="Initialize Data-Base" Style="{DynamicResource mainButtons}" Grid.Row="1" Margin="5,12" Click="initiateDataBase_btnClick"/>
            <Button Content="Reset Data-Base" Style="{DynamicResource mainButtons}" Grid.Row="2" Margin="5,12" Click="resetDataBase_btnClick"/>
        </Grid>

    </Grid>
</Window>
